# Rules for the code shrinker/obfuscator.
#
# Shrinking is currently switched off (see isMinifyEnabled in build.gradle.kts),
# so this file is intentionally empty. It exists because the release build type
# references it, and because rules will be needed here if we ever turn shrinking on.
